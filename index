<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>REGENAHS - Aceite de Oliva Extra Virgen Premium</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary: #2d3e2f;
            --secondary: #7a9b76;
            --accent: #c4a676;
            --dark: #1a1a1a;
            --light: #f5f5f0;
            --white: #ffffff;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            color: var(--dark);
            background: var(--white);
            overflow-x: hidden;
            line-height: 1.6;
        }

        nav {
            position: fixed;
            top: 0;
            width: 100%;
            padding: 2rem 5%;
            background: transparent;
            z-index: 1000;
            transition: all 0.4s;
        }

        nav.scrolled {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            padding: 1.2rem 5%;
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.05);
        }

        nav .container {
            max-width: 1600px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-family: 'Georgia', serif;
            font-size: 2rem;
            font-weight: 300;
            color: var(--white);
            letter-spacing: 8px;
            cursor: pointer;
        }

        nav.scrolled .logo {
            color: var(--primary);
        }

        nav ul {
            display: flex;
            list-style: none;
            gap: 3rem;
        }

        nav a {
            text-decoration: none;
            color: var(--white);
            font-size: 0.9rem;
            letter-spacing: 2px;
            text-transform: uppercase;
            transition: all 0.3s;
            position: relative;
            padding: 0.5rem 0;
        }

        nav.scrolled a {
            color: var(--primary);
        }

        nav a::before {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 0;
            height: 2px;
            background: var(--accent);
            transition: width 0.3s;
        }

        nav a:hover::before {
            width: 100%;
        }

        .hamburger {
            display: none;
            flex-direction: column;
            cursor: pointer;
            gap: 6px;
        }

        .hamburger span {
            width: 30px;
            height: 2px;
            background: var(--white);
            transition: all 0.3s;
        }

        nav.scrolled .hamburger span {
            background: var(--primary);
        }

        .hero {
            height: 100vh;
            position: relative;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }

        .hero-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('https://images.unsplash.com/photo-1474979266404-7eaacbcd87c5?w=1920');
            background-size: cover;
            background-position: center;
            filter: brightness(0.7);
        }

        .hero-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(45, 62, 47, 0.6) 0%, rgba(26, 26, 26, 0.4) 100%);
        }

        .hero-content {
            position: relative;
            z-index: 2;
            text-align: center;
            color: var(--white);
            max-width: 1000px;
            padding: 2rem;
        }

        .hero-subtitle {
            font-size: clamp(0.9rem, 2vw, 1.1rem);
            letter-spacing: 4px;
            text-transform: uppercase;
            margin-bottom: 1.5rem;
            opacity: 0.9;
        }

        .hero h1 {
            font-size: clamp(3rem, 8vw, 7rem);
            font-weight: 300;
            letter-spacing: 8px;
            margin-bottom: 2rem;
            line-height: 1.2;
            font-family: 'Georgia', serif;
        }

        .hero-description {
            font-size: clamp(1rem, 2.5vw, 1.3rem);
            margin-bottom: 3rem;
            opacity: 0.95;
            max-width: 700px;
            margin-left: auto;
            margin-right: auto;
        }

        .hero-cta {
            display: inline-flex;
            gap: 2rem;
            flex-wrap: wrap;
            justify-content: center;
        }

        .btn-primary {
            padding: 1.2rem 3rem;
            background: var(--accent);
            color: var(--white);
            text-decoration: none;
            font-size: 0.9rem;
            letter-spacing: 2px;
            text-transform: uppercase;
            transition: all 0.3s;
            border: 2px solid var(--accent);
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 40px rgba(196, 166, 118, 0.4);
        }

        .btn-secondary {
            padding: 1.2rem 3rem;
            background: transparent;
            color: var(--white);
            text-decoration: none;
            font-size: 0.9rem;
            letter-spacing: 2px;
            text-transform: uppercase;
            border: 2px solid var(--white);
            transition: all 0.3s;
        }

        .btn-secondary:hover {
            background: var(--white);
            color: var(--primary);
            transform: translateY(-3px);
        }

        .story {
            padding: 0;
            background: var(--light);
        }

        .story-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            min-height: 100vh;
        }

        .story-image-container {
            position: relative;
            overflow: hidden;
        }

        .story-image-container img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.6s;
        }

        .story-image-container:hover img {
            transform: scale(1.05);
        }

        .story-content {
            display: flex;
            flex-direction: column;
            justify-content: center;
            padding: 8rem 6rem;
            background: var(--white);
        }

        .story-label {
            font-size: 0.85rem;
            letter-spacing: 3px;
            text-transform: uppercase;
            color: var(--accent);
            margin-bottom: 1.5rem;
            font-weight: 600;
        }

        .story h2 {
            font-size: clamp(2.5rem, 5vw, 4rem);
            font-weight: 300;
            color: var(--primary);
            margin-bottom: 2rem;
            line-height: 1.2;
            font-family: 'Georgia', serif;
        }

        .story p {
            font-size: clamp(1rem, 2vw, 1.15rem);
            line-height: 1.9;
            color: var(--dark);
            margin-bottom: 1.5rem;
            opacity: 0.9;
        }

        .story-stats {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 2rem;
            margin-top: 3rem;
            padding-top: 3rem;
            border-top: 1px solid rgba(45, 62, 47, 0.1);
        }

        .stat {
            text-align: center;
        }

        .stat-number {
            font-size: 3rem;
            font-weight: 300;
            color: var(--accent);
            font-family: 'Georgia', serif;
            display: block;
            margin-bottom: 0.5rem;
        }

        .stat-label {
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 2px;
            color: var(--primary);
        }

        .products {
            padding: 8rem 5%;
            background: var(--white);
        }

        .section-header {
            text-align: center;
            max-width: 800px;
            margin: 0 auto 6rem;
        }

        .section-label {
            font-size: 0.85rem;
            letter-spacing: 3px;
            text-transform: uppercase;
            color: var(--accent);
            margin-bottom: 1rem;
            font-weight: 600;
        }

        .section-title {
            font-size: clamp(2.5rem, 5vw, 4rem);
            font-weight: 300;
            color: var(--primary);
            margin-bottom: 1.5rem;
            font-family: 'Georgia', serif;
        }

        .section-description {
            font-size: 1.1rem;
            color: var(--dark);
            opacity: 0.8;
            line-height: 1.8;
        }

        .products-grid {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 4rem;
        }

        .product-card {
            background: var(--light);
            overflow: hidden;
            transition: all 0.5s;
            cursor: pointer;
        }

        .product-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 30px 60px rgba(0, 0, 0, 0.15);
        }

        .product-image {
            width: 100%;
            height: 450px;
            overflow: hidden;
            position: relative;
        }

        .product-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.6s;
        }

        .product-card:hover .product-image img {
            transform: scale(1.1);
        }

        .product-badge {
            position: absolute;
            top: 1.5rem;
            right: 1.5rem;
            background: var(--accent);
            color: var(--white);
            padding: 0.5rem 1.5rem;
            font-size: 0.75rem;
            letter-spacing: 2px;
            text-transform: uppercase;
            font-weight: 600;
        }

        .product-info {
            padding: 3rem;
        }

        .product-info h3 {
            font-size: 2rem;
            color: var(--primary);
            margin-bottom: 1rem;
            font-family: 'Georgia', serif;
        }

        .product-info p {
            font-size: 1rem;
            line-height: 1.8;
            color: var(--dark);
            opacity: 0.8;
            margin-bottom: 2rem;
        }

        .product-features {
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
            margin-bottom: 2rem;
        }

        .feature-tag {
            padding: 0.5rem 1rem;
            background: var(--white);
            border: 1px solid rgba(45, 62, 47, 0.2);
            font-size: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .product-cta {
            display: inline-block;
            padding: 1rem 2.5rem;
            background: var(--primary);
            color: var(--white);
            text-decoration: none;
            font-size: 0.85rem;
            letter-spacing: 2px;
            text-transform: uppercase;
            transition: all 0.3s;
        }

        .product-cta:hover {
            background: var(--accent);
            transform: translateX(5px);
        }

        .payment-methods {
            padding: 6rem 5%;
            background: var(--light);
        }

        .payment-header {
            text-align: center;
            max-width: 800px;
            margin: 0 auto 4rem;
        }

        .payment-header h2 {
            font-size: clamp(2rem, 4vw, 3rem);
            font-weight: 300;
            color: var(--primary);
            margin-bottom: 1rem;
            font-family: 'Georgia', serif;
        }

        .payment-header p {
            font-size: 1.1rem;
            color: var(--dark);
            opacity: 0.8;
        }

        .payment-grid {
            max-width: 1200px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
        }

        .payment-card {
            background: var(--white);
            padding: 2.5rem;
            text-align: center;
            transition: all 0.3s;
            border: 1px solid rgba(45, 62, 47, 0.1);
        }

        .payment-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        }

        .payment-icon {
            font-size: 3rem;
            margin-bottom: 1.5rem;
        }

        .payment-card h3 {
            font-size: 1.4rem;
            color: var(--primary);
            margin-bottom: 1rem;
        }

        .payment-card > p {
            font-size: 0.95rem;
            color: var(--dark);
            opacity: 0.8;
            line-height: 1.6;
            margin-bottom: 1.5rem;
        }

        .payment-details {
            background: var(--light);
            padding: 1.5rem;
            margin-top: 1rem;
        }

        .payment-detail-item {
            display: flex;
            justify-content: space-between;
            padding: 0.8rem 0;
            border-bottom: 1px solid rgba(45, 62, 47, 0.1);
        }

        .payment-detail-item:last-child {
            border-bottom: none;
        }

        .payment-detail-label {
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: var(--primary);
            font-weight: 600;
        }

        .payment-detail-value {
            font-size: 0.9rem;
            color: var(--dark);
            font-weight: 500;
        }

        .copy-btn {
            display: inline-block;
            margin-top: 1rem;
            padding: 0.8rem 2rem;
            background: var(--primary);
            color: var(--white);
            border: none;
            cursor: pointer;
            font-size: 0.85rem;
            letter-spacing: 1px;
            text-transform: uppercase;
            transition: all 0.3s;
            text-decoration: none;
        }

        .copy-btn:hover {
            background: var(--accent);
            transform: translateY(-2px);
        }

        .payment-note {
            max-width: 900px;
            margin: 4rem auto 0;
            text-align: center;
            padding: 2rem;
            background: rgba(196, 166, 118, 0.1);
            border-left: 4px solid var(--accent);
        }

        .payment-note p {
            font-size: 1rem;
            line-height: 1.8;
        }

        .contact {
            position: relative;
            padding: 10rem 5%;
            background: var(--primary);
            color: var(--white);
            text-align: center;
            overflow: hidden;
        }

        .contact::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('https://i.imgur.com/pYk1UxL.jpg');
            background-size: cover;
            background-position: center;
            opacity: 0.15;
        }

        .contact-content {
            position: relative;
            z-index: 2;
            max-width: 900px;
            margin: 0 auto;
        }

        .contact h2 {
            font-size: clamp(2.5rem, 6vw, 5rem);
            font-weight: 300;
            margin-bottom: 2rem;
            font-family: 'Georgia', serif;
        }

        .contact p {
            font-size: clamp(1rem, 2.5vw, 1.3rem);
            margin-bottom: 4rem;
            opacity: 0.9;
            line-height: 1.8;
        }

        .contact-buttons {
            display: flex;
            gap: 2rem;
            justify-content: center;
            flex-wrap: wrap;
        }

        .contact-btn {
            padding: 1.5rem 3rem;
            background: var(--accent);
            color: var(--white);
            text-decoration: none;
            font-size: 0.9rem;
            letter-spacing: 2px;
            text-transform: uppercase;
            transition: all 0.3s;
            border: 2px solid var(--accent);
        }

        .contact-btn:hover {
            background: transparent;
            transform: translateY(-3px);
        }

        .contact-btn.outline {
            background: transparent;
            border: 2px solid var(--white);
        }

        .contact-btn.outline:hover {
            background: var(--white);
            color: var(--primary);
        }

        footer {
            background: var(--dark);
            color: var(--white);
            padding: 4rem 5%;
        }

        .footer-content {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 2fr 1fr 1fr;
            gap: 4rem;
        }

        .footer-brand h3 {
            font-family: 'Georgia', serif;
            font-size: 1.8rem;
            letter-spacing: 6px;
            margin-bottom: 1rem;
        }

        .footer-brand p {
            opacity: 0.7;
            line-height: 1.8;
        }

        .footer-links h4 {
            font-size: 0.85rem;
            letter-spacing: 2px;
            text-transform: uppercase;
            margin-bottom: 1.5rem;
            color: var(--accent);
        }

        .footer-links ul {
            list-style: none;
        }

        .footer-links a {
            color: var(--white);
            text-decoration: none;
            opacity: 0.7;
            transition: all 0.3s;
            display: inline-block;
            margin-bottom: 0.8rem;
        }

        .footer-links a:hover {
            opacity: 1;
            transform: translateX(5px);
            color: var(--accent);
        }

        .footer-bottom {
            max-width: 1400px;
            margin: 3rem auto 0;
            padding-top: 2rem;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            text-align: center;
            opacity: 0.6;
            font-size: 0.85rem;
        }

        @media (max-width: 1024px) {
            .story-grid {
                grid-template-columns: 1fr;
            }
            .story-image-container {
                height: 60vh;
            }
            .story-content {
                padding: 4rem 3rem;
            }
            .products-grid {
                grid-template-columns: 1fr;
            }
            .footer-content {
                grid-template-columns: 1fr;
            }
        }

        @media (max-width: 768px) {
            nav ul {
                position: fixed;
                top: 0;
                left: -100%;
                width: 80%;
                height: 100vh;
                background: rgba(45, 62, 47, 0.98);
                flex-direction: column;
                justify-content: center;
                padding: 3rem;
                gap: 2rem;
                transition: left 0.4s;
            }
            nav ul.active {
                left: 0;
            }
            nav ul a {
                color: var(--white);
                font-size: 1.2rem;
            }
            .hamburger {
                display: flex;
            }
            .story-stats {
                grid-template-columns: 1fr;
            }
            .contact-buttons {
                flex-direction: column;
            }
        }
    </style>
</head>
<body>
    <nav id="navbar">
        <div class="container">
            <div class="logo" onclick="window.scrollTo({top: 0, behavior: 'smooth'})">REGENAHS</div>
            <ul id="navMenu">
                <li><a href="#inicio">Inicio</a></li>
                <li><a href="#historia">Historia</a></li>
                <li><a href="#productos">Productos</a></li>
                <li><a href="#medios-pago">Medios de Pago</a></li>
                <li><a href="#contacto">Contacto</a></li>
            </ul>
            <div class="hamburger" id="hamburger">
                <span></span>
                <span></span>
                <span></span>
            </div>
        </div>
    </nav>

    <section class="hero" id="inicio">
        <div class="hero-bg"></div>
        <div class="hero-overlay"></div>
        <div class="hero-content">
            <p class="hero-subtitle">San Rafael, Mendoza</p>
            <h1>La autenticidad<br>de lo simple</h1>
            <p class="hero-description">
                Aceite de oliva extra virgen, prensado en frío, elaborado con la tradición de generaciones y el cuidado que solo una familia puede ofrecer.
            </p>
            <div class="hero-cta">
                <a href="#productos" class="btn-primary">Explorar Productos</a>
                <a href="#historia" class="btn-secondary">Nuestra Historia</a>
            </div>
        </div>
    </section>

    <section class="story" id="historia">
        <div class="story-grid">
            <div class="story-image-container">
                <img src="https://images.unsplash.com/photo-1474979266404-7eaacbcd87c5?w=1000" alt="Olivares">
            </div>
            <div class="story-content">
                <p class="story-label">Nuestra Historia</p>
                <h2>De la finca de la abuela a tu mesa</h2>
                <p>Somos un negocio familiar que nace del corazón de San Rafael, Mendoza, donde nuestra abuela ha cultivado olivos durante décadas con dedicación y amor.</p>
                <p>Cada botella de nuestro aceite extra virgen, prensado en frío, lleva consigo la tradición olivícola mendocina y el cuidado artesanal que solo una producción familiar puede ofrecer.</p>
                <p>Más que vender aceite, queremos acompañarte en tu cocina, en tus reuniones familiares y en cada comida compartida.</p>
                
                <div class="story-stats">
                    <div class="stat">
                        <span class="stat-number">50+</span>
                        <span class="stat-label">Años de tradición</span>
                    </div>
                    <div class="stat">
                        <span class="stat-number">100%</span>
                        <span class="stat-label">Extra Virgen</span>
                    </div>
                    <div class="stat">
                        <span class="stat-number">5★</span>
                        <span class="stat-label">Calidad Premium</span>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="products" id="productos">
        <div class="section-header">
            <p class="section-label">Productos</p>
            <h2 class="section-title">Calidad premium en cada formato</h2>
            <p class="section-description">
                Nuestro aceite de oliva extra virgen está disponible en dos presentaciones diseñadas para acompañarte en cada momento.
            </p>
        </div>

        <div class="products-grid">
            <div class="product-card">
                <div class="product-image">
                    <img src="https://images.unsplash.com/photo-1474979266404-7eaacbcd87c5?w=600" alt="Botella 500ml">
                    <div class="product-badge">Premium</div>
                </div>
                <div class="product-info">
                    <h3>Botella 500ml</h3>
                    <p>Diseño estético y sofisticado, ideal para tu mesa diaria. Perfecta para quienes buscan calidad en cada detalle.</p>
                    
                    <div class="product-features">
                        <span class="feature-tag">Extra Virgen</span>
                        <span class="feature-tag">Prensado en Frío</span>
                        <span class="feature-tag">Premium</span>
                    </div>
                    
                    <a href="#contacto" class="product-cta">Consultar →</a>
                </div>
            </div>

            <div class="product-card">
                <div class="product-image">
                    <img src="https://images.unsplash.com/photo-1474979266404-7eaacbcd87c5?w=600" alt="Bidón 3L">
                    <div class="product-badge">Refill</div>
                </div>
                <div class="product-info">
                    <h3>Bidón 3 litros</h3>
                    <p>Sistema innovador de reposición para que recargues tus botellas. Propuesta ecológica y sustentable.</p>
                    
                    <div class="product-features">
                        <span class="feature-tag">Sustentable</span>
                        <span class="feature-tag">Económico</span>
                        <span class="feature-tag">Eco-Friendly</span>
                    </div>
                    
                    <a href="#contacto" class="product-cta">Consultar →</a>
                </div>
            </div>
        </div>
    </section>

    <section class="payment-methods" id="medios-pago">
        <div class="payment-header">
            <h2>Medios de Pago</h2>
            <p>Elegí la opción que más te convenga para realizar tu compra</p>
        </div>

        <div class="payment-grid">
            <div class="payment-card">
                <div class="payment-icon">🏦</div>
                <h3>Transferencia Bancaria</h3>
                <p>Realizá tu transferencia directamente a nuestra cuenta bancaria</p>
                <div class="payment-details">
                    <div class="payment-detail-item">
                        <span class="payment-detail-label">Banco</span>
                        <span class="payment-detail-value">Banco Galicia</span>
                    </div>
                    <div class="payment-detail-item">
                        <span class="payment-detail-label">CBU</span>
                        <span class="payment-detail-value">0070999530004081467701</span>
                    </div>
                    <div class="payment-detail-item">
                        <span class="payment-detail-label">Alias</span>
                        <span class="payment-detail-value">REGENAHS.ACEITE</span>
                    </div>
                    <div class="payment-detail-item">
                        <span class="payment-detail-label">Titular</span>
                        <span class="payment-detail-value">Agustina S. Heredia</span>
                    </div>
                </div>
                <button class="copy-btn" onclick="copyToClipboard('0000003100092695803089', this)">Copiar CVU</button>
            </div>

            <div class="payment-card">
                <div class="payment-icon">💵</div>
                <h3>Efectivo</h3>
                <p>Coordiná la entrega y abonás en el momento</p>
                <div class="payment-details">
                    <div class="payment-detail-item">
                        <span class="payment-detail-label">Zona</span>
                        <span class="payment-detail-value">San Rafael, MDZ</span>
                    </div>
                    <div class="payment-detail-item">
                        <span class="payment-detail-label">Coordinación</span>
                        <span class="payment-detail-value">Por WhatsApp</span>
                    </div>
                </div>
                <a href="https://wa.me/5491112345678?text=Hola!%20Quiero%20coordinar%20pago%20en%20efectivo" class="copy-btn">Contactar</a>
            </div>
        </div>

        <div class="payment-note">
            <p><strong>Importante:</strong> Una vez realizado el pago, envianos el comprobante por WhatsApp indicando tu nombre y dirección de entrega. Procesamos los pedidos en 24-48hs hábiles.</p>
        </div>
    </section>

    <section class="contact" id="contacto">
        <div class="contact-content">
            <h2>Llevá la autenticidad de Mendoza a tu mesa</h2>
            <p>¿Querés probar nuestro aceite premium o necesitás un regalo corporativo exclusivo? Escribinos y descubrí la diferencia.</p>
            
            <div class="contact-buttons">
                <a href="https://wa.me/5491112345678?text=Hola!%20Quiero%20saber%20más%20sobre%20REGENAHS" class="contact-btn" target="_blank">WhatsApp</a>
                <a href="https://instagram.com/tu_usuario" class="contact-btn outline" target="_blank">Instagram</a>
                <a href="mailto:hola@regenahs.com" class="contact-btn outline">Email</a>
            </div>
        </div>
    </section>

    <footer>
        <div class="footer-content">
            <div class="footer-brand">
                <h3>REGENAHS</h3>
                <p>Aceite de oliva extra virgen de San Rafael, Mendoza. Tradición familiar, calidad premium y compromiso con la sustentabilidad.</p>
            </div>
            <div class="footer-links">
                <h4>Enlaces</h4>
                <ul>
                    <li><a href="#inicio">Inicio</a></li>
                    <li><a href="#historia">Historia</a></li>
                    <li><a href="#productos">Productos</a></li>
                    <li><a href="#medios-pago">Medios de Pago</a></li>
                    <li><a href="#contacto">Contacto</a></li>
                </ul>
            </div>
            <div class="footer-links">
                <h4>Contacto</h4>
                <ul>
                    <li><a href="https://wa.me/5491112345678" target="_blank">WhatsApp</a></li>
                    <li><a href="https://instagram.com/tu_usuario" target="_blank">Instagram</a></li>
                    <li><a href="mailto:hola@regenahs.com">Email</a></li>
                </ul>
            </div>
        </div>
        <div class="footer-bottom">
            <p>&copy; 2024 REGENAHS - Aceite de Oliva Extra Virgen | San Rafael, Mendoza</p>
        </div>
    </footer>

    <script>
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({behavior: 'smooth'});
                    document.getElementById('navMenu').classList.remove('active');
                }
            });
        });

        const hamburger = document.getElementById('hamburger');
        const navMenu = document.getElementById('navMenu');
        hamburger.addEventListener('click', () => {
            hamburger.classList.toggle('active');
            navMenu.classList.toggle('active');
        });

        window.addEventListener('scroll', () => {
            const navbar = document.getElementById('navbar');
            if (window.pageYOffset > 100) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });

        function copyToClipboard(text, button) {
            navigator.clipboard.writeText(text).then(() => {
                const originalText = button.textContent;
                button.textContent = '✓ Copiado!';
                button.style.background = 'var(--secondary)';
                setTimeout(() => {
                    button.textContent = originalText;
                    button.style.background = 'var(--primary)';
                }, 2000);
            }).catch(err => {
                alert('No se pudo copiar. Cópialo manualmente.');
            });
        }
    </script>
</body>
</html></span>
                        <span class="payment-detail-value">REGENAHS SRL</span>
                    </div>
                </div>
                <button class="copy-btn" onclick="copyToClipboard('0070999530004081467701', this)">Copiar CBU</button>
            </div>

            <div class="payment-card">
                <div class="payment-icon">💳</div>
                <h3>Mercado Pago</h3>
                <p>Pagá con tarjeta de crédito, débito o dinero en cuenta</p>
                <div class="payment-details">
                    <div class="payment-detail-item">
                        <span class="payment-detail-label">CVU</span>
                        <span class="payment-detail-value">0000003100092695803089</span>
                    </div>
                    <div class="payment-detail-item">
                        <span class="payment-detail-label">Alias</span>
                        <span class="payment-detail-value">regenahs.mp</span>
                    </div>
                    <div class="payment-detail-item">
                        <span class="payment-detail-label">Titular
